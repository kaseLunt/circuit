/**
 * How long a transient notice stands before it retires.
 *
 * ONE constant for every transient the composer speaks through — the canvas field's
 * refused-drop strip and the chrome band's copy confirmation. Two constants that happen to
 * agree today is drift with a date on it: the first time one is tuned, the interface starts
 * speaking at two speeds for no reason a reader could name.
 *
 * Not a token: this is a dwell time, not a motion duration. The transitions that carry a
 * notice in and out are `--motion-fast` from `globals.css`; how long it stays is a reading
 * budget, and CSS has no opinion about reading.
 */
export const NOTICE_DURATION_MS = 4_000;
